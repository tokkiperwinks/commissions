---
import "../styles/colors.css";
---

<style>
.intro-wrapper {
  position: relative;
  width: 100%;
  max-width: 600px;
  margin: 3.2rem auto 0;
}

.intro-shadow {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: var(--color-shadow);
  border-radius: 1rem;
  transform: translateY(8px);
  z-index: 0;
}

.intro-container {
  position: relative;
  padding: 2rem;
  background-color: var(--color-bg);
  border-radius: 1rem;
  font-family: 'Fredoka', sans-serif;
  z-index: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1.5rem;
}

.intro-heading {
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 0.5rem;
  text-align: center;
  color: var(--color-text);
}

.intro-subheading {
  font-size: 1.25rem;
  font-weight: 400;
  color: var(--color-text-light);
  line-height: 1.75;
  text-align: justify;
  margin-bottom: 1rem;
}

/* Social buttons */
.social-buttons {
  display: flex;
  justify-content: center;
  gap: 1.5rem;
  flex-wrap: wrap;
}

.social-button {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  background: var(--color-shadow);
  color: var(--color-text-inverted);
  border-radius: 9999px;
  padding: 0.75rem 1.2rem;
  font-size: 0.85rem;
  font-weight: 500;
  cursor: pointer;
  transition: transform 0.2s ease;
  white-space: nowrap;
  text-decoration: none;
  position: relative; /* ✅ so tooltip positions relative to button */
}

.social-button:hover {
  transform: scale(1.05);
}

.social-button .icon {
  width: 1.5rem;
  height: 1.5rem;
  object-fit: contain;
  display: block;
}

.copy-tooltip {
  position: absolute;
  top: -2.8rem;
  left: 50%;
  transform: translateX(-50%);
  background: var(--color-tooltip);
  color: var(--color-tooltip-text);
  padding: 0.5rem 0.75rem;
  font-size: 0.75rem;
  border-radius: 0.5rem;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.3s ease;
  white-space: nowrap;
  z-index: 10;
}

.copy-tooltip.show {
  opacity: 1;
}

.copy-tooltip::after {
  content: "";
  position: absolute;
  top: 90%;
  left: 50%;
  transform: translateX(-50%);
  border-width: 14px;
  border-style: solid;
  border-color: var(--color-tooltip) transparent transparent transparent;
}

  /* Mobile adjustments */
  @media (max-width: 640px) {
    .social-buttons {
      flex-direction: column;
      align-items: center;
      width: 50%;
	  transform: translateX(-10%);
    }
    .social-button {
      width: 100%;
      justify-content: center;
      text-align: center;
    }
  }
	.tokkigif {
	  width: 100%;          /* Fill the width of the container */
	  max-width: 400px;     /* Optional: limit max width */
	  display: flex;
	  justify-content: center;
	  filter: var(--tokkigif)
	}

	.tokkigif img {
	  width: 100%;          /* Make the image scale to container width */
	  height: auto;         /* Maintain aspect ratio */
	  object-fit: contain;  /* Ensure the whole image fits inside */
	  border-radius: 1rem;  /* Optional: match container style */
	}
</style>

<div class="intro-wrapper">
  <div class="intro-shadow"></div>
  <section class="intro-container">
    <h1 class="intro-heading">Welcome to my commissions page!</h1>
	<div class="tokkigif"><img src="evil tokki.gif" alt="3" /></div>
    <p class="intro-subheading">
	Hihi!! Welcome and thank you for your interest in my art!<br>
	This site is only for price listings and artwork samples,<br>
	please contact me through discord only!<br>
	I will get to you as soon as possible!<br><br>
	
	Check out my Twitter for more artworks! I dont post all of<br>
	them here :o)
	</p>

    <div class="social-buttons">
      <!-- Twitter -->
      <a href="https://x.com/tokkiperiwinkle" target="_blank" class="social-button oblong-button" aria-label="Twitter">
        <img src="twitter.svg" alt="Twitter" class="icon" />
        <span class="social-text">@tokkiperiwinkle</span>
      </a>

      <!-- Discord -->
		<!-- Discord -->
		<a href="#" id="discord-button" class="social-button oblong-button" aria-label="Discord">
		  <img src="discord.svg" alt="Discord" class="icon" />
		  <span class="social-text">tokkiperiwinkle</span>
		  <div id="tooltip" class="copy-tooltip">Copied Username!</div>
		</a>
      </div>
    </div>
  </section>
</div>

<script is:global>
  document.addEventListener("DOMContentLoaded", () => {
    const discordBtn = document.getElementById("discord-button");
    const tooltip = document.getElementById("tooltip");

    if (discordBtn && tooltip) {
      discordBtn.addEventListener("click", (e) => {
        e.preventDefault(); // stop link navigation
        navigator.clipboard.writeText("tokkiperiwinkle")
          .then(() => {
            tooltip.classList.add("show");
            setTimeout(() => tooltip.classList.remove("show"), 1200);
          })
          .catch(err => console.error("Clipboard error:", err));
      });
    }
  });
</script>
